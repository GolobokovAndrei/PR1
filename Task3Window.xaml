<Window x:Class="PR1.Task3Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Задача 3" Height="364" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Введите массив чисел через запятую:" VerticalAlignment="Center" FontSize="16"/>

        <TextBox Grid.Row="1" x:Name="InputArray" Margin="0,10,0,10" FontSize="14" Height="30"/>

        <Button Grid.Row="2" Content="Найти числа" Click="OnFindNumbersClick" HorizontalAlignment="Right" Height="30" Width="120"/>

        <TextBlock Grid.Row="3" Text="Результат:" FontSize="16" Margin="0,10,0,5"/>

        <TextBox Grid.Row="3" x:Name="ResultBox" IsReadOnly="True" FontSize="14" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="100"/>
    </Grid>
</Window>
